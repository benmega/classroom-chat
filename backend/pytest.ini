[pytest]
pythonpath = .
# scratch/ holds manual dev scripts (named test_*.py) that hit a live server at
# import time; they are not CI unit tests and must not be collected.
norecursedirs = venv .venv scratch
addopts = --ignore=test_output.txt --ignore=test_results.txt
