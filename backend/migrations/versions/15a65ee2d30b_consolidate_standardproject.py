"""Consolidate StandardProject

Revision ID: 15a65ee2d30b
Revises: a11cd4c0a073
Create Date: 2026-08-02 19:26:38.160688

"""
import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic.
revision = '15a65ee2d30b'
down_revision = 'a11cd4c0a073'
branch_labels = None
depends_on = None


def upgrade():
    conn = op.get_bind()
    inspector = sa.inspect(conn)

    if 'standard_projects' in inspector.get_table_names():
        op.drop_table('standard_projects')

    existing_cols = [col['name'] for col in inspector.get_columns('project_templates')]
    with op.batch_alter_table('project_templates', schema=None) as batch_op:
        if 'link' not in existing_cols:
            batch_op.add_column(sa.Column('link', sa.String(length=255), nullable=True))
        if 'github_link' not in existing_cols:
            batch_op.add_column(sa.Column('github_link', sa.String(length=255), nullable=True))
        if 'video_url' not in existing_cols:
            batch_op.add_column(sa.Column('video_url', sa.String(length=255), nullable=True))
        if 'code_snippet' not in existing_cols:
            batch_op.add_column(sa.Column('code_snippet', sa.Text(), nullable=True))
        if 'image_url' not in existing_cols:
            batch_op.add_column(sa.Column('image_url', sa.String(length=255), nullable=True))

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('project_templates', schema=None) as batch_op:
        batch_op.drop_column('image_url')
        batch_op.drop_column('code_snippet')
        batch_op.drop_column('video_url')
        batch_op.drop_column('github_link')
        batch_op.drop_column('link')

    op.create_table('standard_projects',
    sa.Column('id', sa.INTEGER(), nullable=False),
    sa.Column('name', sa.VARCHAR(length=100), nullable=False),
    sa.Column('description', sa.TEXT(), nullable=True),
    sa.Column('link', sa.VARCHAR(length=255), nullable=True),
    sa.Column('github_link', sa.VARCHAR(length=255), nullable=True),
    sa.Column('video_url', sa.VARCHAR(length=255), nullable=True),
    sa.Column('code_snippet', sa.TEXT(), nullable=True),
    sa.Column('image_url', sa.VARCHAR(length=255), nullable=True),
    sa.PrimaryKeyConstraint('id', name=op.f('pk_standard_projects'))
    )
    # ### end Alembic commands ###
